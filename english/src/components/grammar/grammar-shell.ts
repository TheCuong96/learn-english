/** Full-width shell cho trang grammar — không dùng `container` để tận dụng hết viewport. */
export const GRAMMAR_SHELL =
  'mx-auto w-full max-w-none px-4 sm:px-6 md:px-8 lg:px-10 xl:px-12 2xl:px-20';
